CUBE_IP=192.168.1.100

# Optional — mirror every state-mutation to additional targets (comma-separated
# host[:port]). Use 127.0.0.1:8765 to also fire at the local mock-cube so the
# Tk overlay shows hook activity even when the real cube is unreachable.
# CUBE_MIRROR=127.0.0.1:8765
